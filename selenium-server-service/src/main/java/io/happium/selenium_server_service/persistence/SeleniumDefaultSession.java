package io.happium.selenium_server_service.persistence;

import javax.persistence.Entity;

/**
 * Selenium DefaultSession instance details
 *
 * <p>
 *     Each Test Pass' details are contained within a DefaultSession
 *     object. The DefaultSession object is then used to populate
 *     the members of this entity class with the corresponding details.
 *     The entity is later saved to the database for record keeping.
 */
@Entity
public class SeleniumDefaultSession {



}
